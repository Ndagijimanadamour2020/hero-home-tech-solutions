'use client'; import { useState } from 'react'; export default function ProjectRequestForm({project}:{project:{id:string,title:string}}){const [done,setDone]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await fetch('/api/project-leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...Object.fromEntries(f),projectId:project.id})});setDone(true)}return <section id="request" className="mt-16 rounded-2xl border border-slate-800 bg-slate-900 p-7"><h2 className="text-2xl font-bold text-white">Request this solution</h2>{done?<p className="mt-3 text-emerald-400">Thanks — we&apos;ll contact you shortly.</p>:<form onSubmit={submit} className="mt-5 grid gap-3 md:grid-cols-2">{['name','business','email','phone','budget'].map(x=><input key={x} required={x==='name'||x==='email'} name={x} placeholder={x[0].toUpperCase()+x.slice(1)} className="rounded-lg border border-slate-700 bg-slate-950 p-3 text-white"/>)}<textarea name="message" placeholder={`Tell us about your needs for ${project.title}`} className="md:col-span-2 rounded-lg border border-slate-700 bg-slate-950 p-3 text-white"/><button className="rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">Send request</button></form>}</section>}
