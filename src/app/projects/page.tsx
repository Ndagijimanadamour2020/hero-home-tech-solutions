export const dynamic='force-dynamic'; import Link from 'next/link'; import { prisma } from '@/lib/prisma'; import { projectPrice } from '@/lib/project'; export default async function Projects(){const projects=await prisma.project.findMany({where:{status:'PUBLISHED'},include:{category:true},orderBy:[{featured:'desc'},{displayOrder:'asc'}]});return <section className="min-h-screen bg-slate-950 py-20"><div className="mx-auto max-w-7xl px-4"><p className="text-xs font-bold uppercase tracking-widest text-blue-400">Our work</p><h1 className="mt-3 text-4xl font-bold text-white">Projects built for real business growth</h1><div className="mt-10 grid gap-6 md:grid-cols-3">{projects.map(p=><article key={p.id} className="overflow-hidden rounded-2xl border border-slate-800 bg-slate-900">{p.image&&<img src={p.image} alt="" className="h-44 w-full object-cover"/>}<div className="p-6"><p className="text-xs text-blue-400">{p.category.name}</p><h2 className="mt-2 text-xl font-bold text-white">{p.title}</h2><p className="mt-3 text-sm text-slate-400">{p.shortDescription}</p><p className="mt-4 font-semibold text-emerald-400">{projectPrice(p)}</p><Link href={`/projects/${p.slug}`} className="mt-5 inline-block text-sm font-semibold text-blue-400">View project →</Link></div></article>)}</div>{!projects.length&&<p className="mt-10 text-slate-400">No projects published yet.</p>}</div></section>}
