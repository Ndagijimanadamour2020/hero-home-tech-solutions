'use client'; import { useState } from 'react'; export default function Checkout({params}:{params:{projectSlug:string}}){const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/payments/pesapal/checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...data,projectSlug:params.projectSlug})});const json=await r.json();if(r.ok)window.location.assign(json.redirectUrl);else setError(json.error||'Unable to start payment.')}return <main className="min-h-screen bg-slate-950 py-20"><form onSubmit={submit} className="mx-auto max-w-lg rounded-2xl border border-slate-800 bg-slate-900 p-8"><p className="text-xs font-bold uppercase tracking-widest text-blue-400">Secure checkout</p><h1 className="mt-2 text-3xl font-bold text-white">Purchase this solution</h1><p className="mt-2 text-sm text-slate-400">Pricing is confirmed securely from Hero Home Tech&apos;s server.</p><div className="mt-6 space-y-4">{['name','email','phone','business'].map(x=><input key={x} required={x!=='business'} name={x} type={x==='email'?'email':'text'} placeholder={x[0].toUpperCase()+x.slice(1)} className="w-full rounded-lg border border-slate-700 bg-slate-950 p-3 text-white"/>)}{error&&<p className="text-sm text-red-400">{error}</p>}<button className="w-full rounded-xl bg-blue-600 py-3 font-bold text-white">Continue to PesaPal</button></div></form></main>}
