export const dynamic='force-dynamic'; import { redirect } from 'next/navigation'; import { isAdmin } from '@/lib/auth'; import { prisma } from '@/lib/prisma'; export default async function Leads(){if(!isAdmin())redirect('/admin/login');const leads=await prisma.lead.findMany({include:{project:true},orderBy:{createdAt:'desc'}});return <main className="p-6 lg:p-10"><h1 className="text-3xl font-bold text-white">Leads / Messages</h1><div className="mt-6 overflow-x-auto rounded-xl border border-slate-800"><table className="w-full text-left text-sm"><thead className="bg-slate-900 text-slate-400"><tr><th className="p-4">Contact</th><th>Business</th><th>Project</th><th>Budget</th><th>Status</th></tr></thead><tbody>{leads.map(l=><tr key={l.id} className="border-t border-slate-800 text-slate-300"><td className="p-4">{l.name}<span className="block text-xs text-slate-500">{l.email}</span></td><td>{l.business}</td><td>{l.project?.title||l.type}</td><td>{l.budget||'—'}</td><td>{l.status}</td></tr>)}</tbody></table>{!leads.length&&<p className="p-6 text-slate-400">No customer inquiries yet.</p>}</div></main>}
