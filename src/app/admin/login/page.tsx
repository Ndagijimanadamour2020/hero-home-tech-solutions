'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function LoginPage() { const router = useRouter(); const [error, setError] = useState(''); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const form = new FormData(e.currentTarget); const response = await fetch('/api/admin/login', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ username: form.get('username'), password: form.get('password') }) }); if (response.ok) router.push('/admin'); else setError('Invalid username or password.'); } return <main className="min-h-screen bg-slate-950 grid place-items-center px-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border border-slate-800 bg-slate-900 p-8"><p className="text-blue-400 text-xs font-bold uppercase tracking-widest">Hero Home Tech</p><h1 className="text-3xl font-bold text-white mt-2">Admin sign in</h1><p className="text-slate-400 text-sm mt-2">Manage your website and incoming leads.</p><div className="mt-8 space-y-4"><input required name="username" placeholder="Username" className="w-full rounded-xl bg-slate-950 border border-slate-700 p-3 text-white"/><input required type="password" name="password" placeholder="Password" className="w-full rounded-xl bg-slate-950 border border-slate-700 p-3 text-white"/>{error && <p className="text-red-400 text-sm">{error}</p>}<button className="w-full rounded-xl bg-blue-600 py-3 font-bold text-white">Sign in</button></div></form></main>; }
