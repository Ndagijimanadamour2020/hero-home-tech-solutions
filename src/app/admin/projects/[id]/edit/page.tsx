export const dynamic='force-dynamic'; import { redirect,notFound } from 'next/navigation'; import { isAdmin } from '@/lib/auth'; import { prisma } from '@/lib/prisma'; import ProjectForm from '@/components/ProjectForm'; export default async function Edit({params}:{params:{id:string}}){if(!isAdmin())redirect('/admin/login');const project=await prisma.project.findUnique({where:{id:params.id}});if(!project)notFound();return <main className="p-6 lg:p-10"><h1 className="mb-6 text-3xl font-bold text-white">Edit Project</h1><ProjectForm project={{...project,price:project.price?.toString()}}/></main>}
