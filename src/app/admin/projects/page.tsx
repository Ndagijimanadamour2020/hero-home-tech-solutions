export const dynamic='force-dynamic'; import Link from 'next/link'; import { redirect } from 'next/navigation'; import { isAdmin } from '@/lib/auth'; import { prisma } from '@/lib/prisma'; import { projectPrice } from '@/lib/project'; export default async function Projects(){if(!isAdmin())redirect('/admin/login');const projects=await prisma.project.findMany({include:{category:true},orderBy:{createdAt:'desc'}});return <main className="p-6 lg:p-10"><div className="flex justify-between"><h1 className="text-3xl font-bold text-white">Projects</h1><Link href="/admin/projects/new" className="rounded-xl bg-blue-600 px-4 py-2 font-semibold text-white">Add Project</Link></div><div className="mt-6 overflow-x-auto rounded-xl border border-slate-800"><table className="w-full text-left text-sm"><thead className="bg-slate-900 text-slate-400"><tr><th className="p-4">Project</th><th>Category</th><th>Price</th><th>Status</th><th>Featured</th><th></th></tr></thead><tbody>{projects.map(p=><tr key={p.id} className="border-t border-slate-800 text-slate-300"><td className="p-4 font-semibold">{p.title}</td><td>{p.category.name}</td><td>{projectPrice(p)}</td><td>{p.status}</td><td>{p.featured?'Yes':'No'}</td><td><Link href={`/projects/${p.slug}`} className="mr-3 text-blue-400">View</Link><Link href={`/admin/projects/${p.id}/edit`} className="text-blue-400">Edit</Link></td></tr>)}</tbody></table>{!projects.length&&<p className="p-6 text-slate-400">Create your first project.</p>}</div></main>}
