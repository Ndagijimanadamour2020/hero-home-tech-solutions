'use client'; import { useEffect,useState } from 'react'; export default function Categories(){const [items,setItems]=useState<any[]>([]);const [name,setName]=useState('');const load=()=>fetch('/api/admin/categories').then(r=>r.json()).then(setItems);useEffect(()=>{void load()},[]);async function add(e:React.FormEvent){e.preventDefault();await fetch('/api/admin/categories',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name})});setName('');load()}return <main className="p-6 lg:p-10"><h1 className="text-3xl font-bold text-white">Categories</h1><form onSubmit={add} className="mt-6 flex max-w-lg gap-3"><input value={name} onChange={e=>setName(e.target.value)} required placeholder="e.g. E-commerce" className="flex-1 rounded-lg border border-slate-700 bg-slate-900 p-3 text-white"/><button className="rounded-lg bg-blue-600 px-4 text-white">Add</button></form><div className="mt-6 max-w-lg space-y-2">{items.map(c=><div key={c.id} className="flex justify-between rounded-xl border border-slate-800 bg-slate-900 p-4 text-slate-300"><span>{c.name}</span><span className="text-slate-500">{c._count.projects} projects</span></div>)}</div></main>}
